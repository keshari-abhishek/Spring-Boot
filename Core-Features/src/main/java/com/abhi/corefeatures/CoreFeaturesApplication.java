package com.abhi.corefeatures;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CoreFeaturesApplication {

	public static void main(String[] args) {
		SpringApplication.run(CoreFeaturesApplication.class, args);
		/*
		SpringApplication application = new SpringApplication(CoreFeaturesApplication.class);
		application.setBannerMode(Banner.Mode.OFF);
		application.run(args);
		*/
		
		
	}

}
